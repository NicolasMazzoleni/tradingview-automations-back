const index = async () => {
  console.log("---------------------");
  console.log("running a task every 15 seconds");

  // Update alerts on all cryptos listed to the latest date
};

module.exports = {
  index
}